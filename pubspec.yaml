name: pert6
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ➜ Tambahan untuk membuka file PDF
  flutter_pdfview: ^1.2.7
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

  # ➜ Tambahkan folder asset PDF kamu
  assets:
    - assets/pdf/